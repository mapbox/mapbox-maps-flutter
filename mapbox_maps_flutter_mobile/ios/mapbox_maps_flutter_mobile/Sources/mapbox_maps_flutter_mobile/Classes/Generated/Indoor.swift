// Autogenerated from Pigeon (v25.2.0), do not edit directly.
// See also: https://pub.dev/packages/pigeon

import Foundation

#if os(iOS)
  import Flutter
#elseif os(macOS)
  import FlutterMacOS
#else
  #error("Unsupported platform.")
#endif

/// Error class for passing custom error details to Dart side.
final class IndoorError: Error {
  let code: String
  let message: String?
  let details: Sendable?

  init(code: String, message: String?, details: Sendable?) {
    self.code = code
    self.message = message
    self.details = details
  }

  var localizedDescription: String {
    return
      "IndoorError(code: \(code), message: \(message ?? "<nil>"), details: \(details ?? "<nil>")"
  }
}

private func wrapResult(_ result: Any?) -> [Any?] {
  return [result]
}

private func wrapError(_ error: Any) -> [Any?] {
  if let pigeonError = error as? IndoorError {
    return [
      pigeonError.code,
      pigeonError.message,
      pigeonError.details,
    ]
  }
  if let flutterError = error as? FlutterError {
    return [
      flutterError.code,
      flutterError.message,
      flutterError.details,
    ]
  }
  return [
    "\(error)",
    "\(type(of: error))",
    "Stacktrace: \(Thread.callStackSymbols)",
  ]
}

private func isNullish(_ value: Any?) -> Bool {
  return value is NSNull || value == nil
}

private func nilOrValue<T>(_ value: Any?) -> T? {
  if value is NSNull { return nil }
  return value as! T?
}

private class IndoorPigeonCodecReader: FlutterStandardReader {
}

private class IndoorPigeonCodecWriter: FlutterStandardWriter {
}

private class IndoorPigeonCodecReaderWriter: FlutterStandardReaderWriter {
  override func reader(with data: Data) -> FlutterStandardReader {
    return IndoorPigeonCodecReader(data: data)
  }

  override func writer(with data: NSMutableData) -> FlutterStandardWriter {
    return IndoorPigeonCodecWriter(data: data)
  }
}

class IndoorPigeonCodec: FlutterStandardMessageCodec, @unchecked Sendable {
  static let shared = IndoorPigeonCodec(readerWriter: IndoorPigeonCodecReaderWriter())
}

/// Generated protocol from Pigeon that represents a handler of messages from Flutter.
protocol _IndoorMessenger {
  func selectFloor(floorId: String?) throws
}

/// Generated setup class from Pigeon to handle messages through the `binaryMessenger`.
class _IndoorMessengerSetup {
  static var codec: FlutterStandardMessageCodec { IndoorPigeonCodec.shared }
  /// Sets up an instance of `_IndoorMessenger` to handle messages through the `binaryMessenger`.
  static func setUp(binaryMessenger: FlutterBinaryMessenger, api: _IndoorMessenger?, messageChannelSuffix: String = "") {
    let channelSuffix = messageChannelSuffix.count > 0 ? ".\(messageChannelSuffix)" : ""
    let selectFloorChannel = FlutterBasicMessageChannel(name: "dev.flutter.pigeon.mapbox_maps_flutter._IndoorMessenger.selectFloor\(channelSuffix)", binaryMessenger: binaryMessenger, codec: codec)
    if let api = api {
      selectFloorChannel.setMessageHandler { message, reply in
        let args = message as! [Any?]
        let floorIdArg: String? = nilOrValue(args[0])
        do {
          try api.selectFloor(floorId: floorIdArg)
          reply(wrapResult(nil))
        } catch {
          reply(wrapError(error))
        }
      }
    } else {
      selectFloorChannel.setMessageHandler(nil)
    }
  }
}
