// Autogenerated from Pigeon (v25.2.0), do not edit directly.
// See also: https://pub.dev/packages/pigeon
@file:Suppress("UNCHECKED_CAST", "ArrayInDataClass")

package com.mapbox.maps.mapbox_maps.pigeons

import android.util.Log
import io.flutter.plugin.common.BasicMessageChannel
import io.flutter.plugin.common.BinaryMessenger
import io.flutter.plugin.common.MessageCodec
import io.flutter.plugin.common.StandardMessageCodec
import java.io.ByteArrayOutputStream
import java.nio.ByteBuffer

private fun wrapResult(result: Any?): List<Any?> {
  return listOf(result)
}

private fun wrapError(exception: Throwable): List<Any?> {
  return if (exception is FlutterError) {
    listOf(
      exception.code,
      exception.message,
      exception.details
    )
  } else {
    listOf(
      exception.javaClass.simpleName,
      exception.toString(),
      "Cause: " + exception.cause + ", Stacktrace: " + Log.getStackTraceString(exception)
    )
  }
}

/**
 * Memory statistics for debugging memory leaks
 *
 * Generated class from Pigeon that represents data sent in messages.
 */
data class MemoryStatistics(
  /** Currently used memory in bytes (Java heap) */
  val usedMemoryBytes: Long,
  /** Total memory allocated by the runtime in bytes (Java heap) */
  val totalMemoryBytes: Long,
  /** Free memory available in the runtime in bytes (Java heap) */
  val freeMemoryBytes: Long,
  /** Maximum memory the runtime can use in bytes (Java heap) */
  val maxMemoryBytes: Long,
  /** Native heap size in bytes (C++ allocations) */
  val nativeHeapBytes: Long,
  /** Native heap allocated size in bytes */
  val nativeHeapAllocatedBytes: Long
) {
  companion object {
    fun fromList(pigeonVar_list: List<Any?>): MemoryStatistics {
      val usedMemoryBytes = pigeonVar_list[0] as Long
      val totalMemoryBytes = pigeonVar_list[1] as Long
      val freeMemoryBytes = pigeonVar_list[2] as Long
      val maxMemoryBytes = pigeonVar_list[3] as Long
      val nativeHeapBytes = pigeonVar_list[4] as Long
      val nativeHeapAllocatedBytes = pigeonVar_list[5] as Long
      return MemoryStatistics(usedMemoryBytes, totalMemoryBytes, freeMemoryBytes, maxMemoryBytes, nativeHeapBytes, nativeHeapAllocatedBytes)
    }
  }
  fun toList(): List<Any?> {
    return listOf(
      usedMemoryBytes,
      totalMemoryBytes,
      freeMemoryBytes,
      maxMemoryBytes,
      nativeHeapBytes,
      nativeHeapAllocatedBytes,
    )
  }
  override fun equals(other: Any?): Boolean {
    if (other !is MemoryStatistics) {
      return false
    }
    if (this === other) {
      return true
    }
    return usedMemoryBytes == other.usedMemoryBytes &&
      totalMemoryBytes == other.totalMemoryBytes &&
      freeMemoryBytes == other.freeMemoryBytes &&
      maxMemoryBytes == other.maxMemoryBytes &&
      nativeHeapBytes == other.nativeHeapBytes &&
      nativeHeapAllocatedBytes == other.nativeHeapAllocatedBytes
  }

  override fun hashCode(): Int = toList().hashCode()
}
private open class MemoryStatisticsPigeonCodec : StandardMessageCodec() {
  override fun readValueOfType(type: Byte, buffer: ByteBuffer): Any? {
    return when (type) {
      129.toByte() -> {
        return (readValue(buffer) as? List<Any?>)?.let {
          MemoryStatistics.fromList(it)
        }
      }
      else -> super.readValueOfType(type, buffer)
    }
  }
  override fun writeValue(stream: ByteArrayOutputStream, value: Any?) {
    when (value) {
      is MemoryStatistics -> {
        stream.write(129)
        writeValue(stream, value.toList())
      }
      else -> super.writeValue(stream, value)
    }
  }
}

/**
 * API for getting memory statistics from the platform
 *
 * Generated interface from Pigeon that represents a handler of messages from Flutter.
 */
interface MemoryStatisticsApi {
  /** Get current memory statistics */
  fun getMemoryStatistics(): MemoryStatistics

  companion object {
    /** The codec used by MemoryStatisticsApi. */
    val codec: MessageCodec<Any?> by lazy {
      MemoryStatisticsPigeonCodec()
    }
    /** Sets up an instance of `MemoryStatisticsApi` to handle messages through the `binaryMessenger`. */
    @JvmOverloads
    fun setUp(binaryMessenger: BinaryMessenger, api: MemoryStatisticsApi?, messageChannelSuffix: String = "") {
      val separatedMessageChannelSuffix = if (messageChannelSuffix.isNotEmpty()) ".$messageChannelSuffix" else ""
      run {
        val channel = BasicMessageChannel<Any?>(binaryMessenger, "dev.flutter.pigeon.mapbox_maps_flutter.MemoryStatisticsApi.getMemoryStatistics$separatedMessageChannelSuffix", codec)
        if (api != null) {
          channel.setMessageHandler { _, reply ->
            val wrapped: List<Any?> = try {
              listOf(api.getMemoryStatistics())
            } catch (exception: Throwable) {
              wrapError(exception)
            }
            reply.reply(wrapped)
          }
        } else {
          channel.setMessageHandler(null)
        }
      }
    }
  }
}