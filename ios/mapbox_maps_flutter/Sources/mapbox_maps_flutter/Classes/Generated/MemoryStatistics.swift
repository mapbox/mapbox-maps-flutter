// Autogenerated from Pigeon (v25.2.0), do not edit directly.
// See also: https://pub.dev/packages/pigeon

import Foundation

#if os(iOS)
  import Flutter
#elseif os(macOS)
  import FlutterMacOS
#else
  #error("Unsupported platform.")
#endif

/// Error class for passing custom error details to Dart side.
final class MemoryStatisticsError: Error {
  let code: String
  let message: String?
  let details: Sendable?

  init(code: String, message: String?, details: Sendable?) {
    self.code = code
    self.message = message
    self.details = details
  }

  var localizedDescription: String {
    return
      "MemoryStatisticsError(code: \(code), message: \(message ?? "<nil>"), details: \(details ?? "<nil>")"
  }
}

private func wrapResult(_ result: Any?) -> [Any?] {
  return [result]
}

private func wrapError(_ error: Any) -> [Any?] {
  if let pigeonError = error as? MemoryStatisticsError {
    return [
      pigeonError.code,
      pigeonError.message,
      pigeonError.details,
    ]
  }
  if let flutterError = error as? FlutterError {
    return [
      flutterError.code,
      flutterError.message,
      flutterError.details,
    ]
  }
  return [
    "\(error)",
    "\(type(of: error))",
    "Stacktrace: \(Thread.callStackSymbols)",
  ]
}

private func isNullish(_ value: Any?) -> Bool {
  return value is NSNull || value == nil
}

private func nilOrValue<T>(_ value: Any?) -> T? {
  if value is NSNull { return nil }
  return value as! T?
}

/// Memory statistics for debugging memory leaks
///
/// Generated class from Pigeon that represents data sent in messages.
struct MemoryStatistics {
  /// Currently used memory in bytes (Java heap)
  var usedMemoryBytes: Int64
  /// Total memory allocated by the runtime in bytes (Java heap)
  var totalMemoryBytes: Int64
  /// Free memory available in the runtime in bytes (Java heap)
  var freeMemoryBytes: Int64
  /// Maximum memory the runtime can use in bytes (Java heap)
  var maxMemoryBytes: Int64
  /// Native heap size in bytes (C++ allocations)
  var nativeHeapBytes: Int64
  /// Native heap allocated size in bytes
  var nativeHeapAllocatedBytes: Int64


  // swift-format-ignore: AlwaysUseLowerCamelCase
  static func fromList(_ pigeonVar_list: [Any?]) -> MemoryStatistics? {
    let usedMemoryBytes = pigeonVar_list[0] as! Int64
    let totalMemoryBytes = pigeonVar_list[1] as! Int64
    let freeMemoryBytes = pigeonVar_list[2] as! Int64
    let maxMemoryBytes = pigeonVar_list[3] as! Int64
    let nativeHeapBytes = pigeonVar_list[4] as! Int64
    let nativeHeapAllocatedBytes = pigeonVar_list[5] as! Int64

    return MemoryStatistics(
      usedMemoryBytes: usedMemoryBytes,
      totalMemoryBytes: totalMemoryBytes,
      freeMemoryBytes: freeMemoryBytes,
      maxMemoryBytes: maxMemoryBytes,
      nativeHeapBytes: nativeHeapBytes,
      nativeHeapAllocatedBytes: nativeHeapAllocatedBytes
    )
  }
  func toList() -> [Any?] {
    return [
      usedMemoryBytes,
      totalMemoryBytes,
      freeMemoryBytes,
      maxMemoryBytes,
      nativeHeapBytes,
      nativeHeapAllocatedBytes,
    ]
  }
}

private class MemoryStatisticsPigeonCodecReader: FlutterStandardReader {
  override func readValue(ofType type: UInt8) -> Any? {
    switch type {
    case 129:
      return MemoryStatistics.fromList(self.readValue() as! [Any?])
    default:
      return super.readValue(ofType: type)
    }
  }
}

private class MemoryStatisticsPigeonCodecWriter: FlutterStandardWriter {
  override func writeValue(_ value: Any) {
    if let value = value as? MemoryStatistics {
      super.writeByte(129)
      super.writeValue(value.toList())
    } else {
      super.writeValue(value)
    }
  }
}

private class MemoryStatisticsPigeonCodecReaderWriter: FlutterStandardReaderWriter {
  override func reader(with data: Data) -> FlutterStandardReader {
    return MemoryStatisticsPigeonCodecReader(data: data)
  }

  override func writer(with data: NSMutableData) -> FlutterStandardWriter {
    return MemoryStatisticsPigeonCodecWriter(data: data)
  }
}

class MemoryStatisticsPigeonCodec: FlutterStandardMessageCodec, @unchecked Sendable {
  static let shared = MemoryStatisticsPigeonCodec(readerWriter: MemoryStatisticsPigeonCodecReaderWriter())
}

/// API for getting memory statistics from the platform
///
/// Generated protocol from Pigeon that represents a handler of messages from Flutter.
protocol MemoryStatisticsApi {
  /// Get current memory statistics
  func getMemoryStatistics() throws -> MemoryStatistics
}

/// Generated setup class from Pigeon to handle messages through the `binaryMessenger`.
class MemoryStatisticsApiSetup {
  static var codec: FlutterStandardMessageCodec { MemoryStatisticsPigeonCodec.shared }
  /// Sets up an instance of `MemoryStatisticsApi` to handle messages through the `binaryMessenger`.
  static func setUp(binaryMessenger: FlutterBinaryMessenger, api: MemoryStatisticsApi?, messageChannelSuffix: String = "") {
    let channelSuffix = messageChannelSuffix.count > 0 ? ".\(messageChannelSuffix)" : ""
    /// Get current memory statistics
    let getMemoryStatisticsChannel = FlutterBasicMessageChannel(name: "dev.flutter.pigeon.mapbox_maps_flutter.MemoryStatisticsApi.getMemoryStatistics\(channelSuffix)", binaryMessenger: binaryMessenger, codec: codec)
    if let api = api {
      getMemoryStatisticsChannel.setMessageHandler { _, reply in
        do {
          let result = try api.getMemoryStatistics()
          reply(wrapResult(result))
        } catch {
          reply(wrapError(error))
        }
      }
    } else {
      getMemoryStatisticsChannel.setMessageHandler(nil)
    }
  }
}
