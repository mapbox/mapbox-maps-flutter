org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
# Resolve the dependencies from local sources, when they are available.
useLocalDependencies=false
# NDK major used for local source builds.
ndkMajor=27
# AGP is pinned below the version Flutter's plugin expects, so its dependency
# check is disabled.
skipDependencyChecks=true
