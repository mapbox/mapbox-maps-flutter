name: mapbox_maps_flutter_platform_interface_example
description: Example app for the Mapbox Maps Flutter platform interface.
publish_to: 'none'

environment:
  sdk: ">=3.10.0 <4.0.0"
  flutter: ">=3.38.1"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  mapbox_maps_flutter_platform_interface:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
